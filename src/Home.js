const Home = () => {
    return (
        <div className="my-5 p-3 border-2 border-gray-600">
            Go to "/login" {'->'} To login a user.<br />
            Go to "/register" {'->'} To register/signup a new user.  <br />
            Go to "/dashboard" {'->'} Dashboad of logged-in user <br />
        </div>
    )
}

export default Home
